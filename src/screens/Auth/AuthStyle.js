import { StyleSheet, Dimensions } from 'react-native';

const style = StyleSheet.create({
    contentStyle: {
        width: '100%', height: '100%', alignItems: 'center', flex: 1, justifyContent: 'space-between'
    }
})

export default style